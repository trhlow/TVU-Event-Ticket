# Production (Neon PostgreSQL). All values required from env — no fallback defaults.
spring:
  datasource:
    url: ${EVENT_DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: false

logging:
  level:
    root: INFO
    vn.edu.tvu: INFO
