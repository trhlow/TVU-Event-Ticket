-- Flyway baseline for event-service.
-- Real DDL (events, clubs) will be added here as entities are defined.
-- Migration files are immutable once applied: never edit a shipped Vn__*.sql — add a new version.
SELECT 1;
