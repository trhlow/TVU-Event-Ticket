# Production (Upstash / Redis Cloud for the rate-limiter store)
spring:
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD:}
      ssl:
        enabled: ${REDIS_SSL:true}

logging:
  level:
    root: INFO
    vn.edu.tvu: INFO
