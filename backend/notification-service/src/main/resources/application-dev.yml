# Local development (docker-compose in ../../../infra)
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  mail:
    host: localhost
    port: 1025          # e.g. MailHog / Mailpit for local testing
    username:
    password:

logging:
  level:
    vn.edu.tvu: DEBUG
